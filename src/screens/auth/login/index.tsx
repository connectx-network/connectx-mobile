import {LayoutAuth, Text} from '@components';
import {memo} from 'react';

const LoginScreen = () => {
  return (
    <LayoutAuth>
      <Text>asdf</Text>
    </LayoutAuth>
  );
};

export default memo(LoginScreen);
